server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # Health check
    location = /health {
        access_log off;
        add_header Content-Type text/plain;
        return 200 'OK';
    }

    # ── Outlook Add-in (alias: root-level URLs → outlook/ directory) ──
    location = /taskpane.html {
        alias /usr/share/nginx/html/outlook/taskpane.html;
        add_header Cache-Control "no-cache, must-revalidate";
    }
    location = /commands.html {
        alias /usr/share/nginx/html/outlook/commands.html;
        add_header Cache-Control "no-cache, must-revalidate";
    }
    location /assets/ {
        alias /usr/share/nginx/html/outlook/assets/;
        expires 7d;
        add_header Cache-Control "public";
    }
    location /src/ {
        alias /usr/share/nginx/html/outlook/src/;
        add_header Cache-Control "no-cache, must-revalidate";
    }

    # ── Frontend Admin Panel (root-relative: /admin/ maps to admin/) ──
    location /admin/ {
        try_files $uri $uri/ /admin/index.html;
        add_header Cache-Control "no-cache, must-revalidate";
    }
    location = /admin {
        return 301 /admin/;
    }

    # ── Scanner App (root-relative: /scanner/ maps to scanner/) ──
    location /scanner/ {
        try_files $uri $uri/ /scanner/index.html;
    }
    location = /scanner {
        return 301 /scanner/;
    }

    # Serve .mjs files with correct JavaScript MIME type (PDF.js worker)
    location ~* \.mjs$ {
        types { }
        default_type text/javascript;
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Cache static assets for scanner and admin
    location ~* ^/(scanner|admin)/.*\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|mjs)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
